--- multi_dec_infer_enc/openvino_yolo.cpp
+++ multi_dec_infer_enc/openvino_yolo.cpp
@@ -123,8 +123,8 @@ std::pair<std::vector<cv::Rect>, std::vector<int>> OpenvinoYolo::postprocess(con
     auto output_shape = output.get_shape();
     int rows = output_shape[2];
     int dimensions = output_shape[1];
-    float* data = output.data<float>();
-    Mat output_buffer(output_shape[1], output_shape[2], CV_32F, data);
+    const float* data = output.data<const float>();
+    Mat output_buffer(output_shape[1], output_shape[2], CV_32F, const_cast<float*>(data));
     transpose(output_buffer, output_buffer);
     float score_threshold = 0.4;
     float nms_threshold = 0.7;
@@ -178,8 +178,8 @@ std::pair<std::vector<cv::Rect>, std::vector<int>> OpenvinoYolo::postprocess(con
 
 std::pair<std::vector<cv::Rect>, std::vector<int>> OpenvinoYolo::postprocesswithoutscale(const ov::Tensor &output) {
     auto output_shape = output.get_shape();
-    float* data = output.data<float>();
-    Mat output_buffer(output_shape[1], output_shape[2], CV_32F, data);
+    const float* data = output.data<const float>();
+    Mat output_buffer(output_shape[1], output_shape[2], CV_32F, const_cast<float*>(data));
     transpose(output_buffer, output_buffer);
     float score_threshold = 0.4;
     float nms_threshold = 0.7;
